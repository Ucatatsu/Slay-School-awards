# 🏆 Slay School Awards 2025-2026

Современная система голосования для школьных номинаций с защитой от накрутки и красивым дизайном.

![Status](https://img.shields.io/badge/Status-Ready-success)
![Firebase](https://img.shields.io/badge/Database-Firebase-orange)
![GitHub Pages](https://img.shields.io/badge/Deploy-GitHub%20Pages-blue)

## ✨ Возможности

- 🎨 Элегантный дизайн с анимациями
- 🔒 Защита от повторного голосования (localStorage + Firebase fingerprint)
- 📊 Админ-панель с результатами в реальном времени
- 📱 Адаптивный дизайн для всех устройств
- ⚡ Быстрая загрузка с красивым экраном загрузки
- 🎭 Поддержка фотографий кандидатов и изображений номинаций
- 🔥 Firebase Realtime Database

## 🚀 Быстрый старт

### 1. Настройка Firebase

1. Создай проект в [Firebase Console](https://console.firebase.google.com/)
2. Включи Realtime Database
3. Скопируй конфигурацию в `config.js`
4. Установи правила безопасности:

```json
{
  "rules": {
    "votes": {
      ".read": true,
      ".write": "now < 1765990800000",
      "$nomination": {
        "$candidate": {
          ".validate": "newData.isNumber()"
        }
      }
    },
    "userVotes": {
      ".read": false,
      ".write": "now < 1765990800000",
      "$userId": {
        "$nomination": {
          ".validate": "!data.exists() && newData.isString()"
        }
      }
    }
  }
}
```

### 2. Настройка проекта

Отредактируй `config.js`:

```javascript
// Пароль администратора
export const ADMIN_PASSWORD = "твой-пароль";

// Добавь своих кандидатов
export const candidates = [
    { id: "candidate1", name: "Иван Иванов", photo: "assets/candidates/ivan.jpg" },
    { id: "candidate2", name: "Мария Петрова", emoji: "👩" }
];

// Настрой номинации
export const nominations = [
    { id: "best-student", title: "Лучший ученик", emoji: "🎓" }
];
```

### 3. Деплой на GitHub Pages

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/username/repo.git
git push -u origin main
```

Затем в Settings → Pages → Source: **GitHub Actions**

Сайт будет доступен: `https://username.github.io/repo/`

## 📁 Структура проекта

### Production файлы (на GitHub Pages):
```
├── index.html              # Главная страница голосования
├── admin.html              # Админ-панель
├── app.js                  # Логика голосования
├── admin.js                # Логика админ-панели
├── config.js               # Настройки (Firebase, кандидаты, номинации)
├── styles.css              # Основные стили
├── admin-styles.css        # Стили админ-панели
├── image_scales.css        # Стили для изображений
├── toast.css               # Стили уведомлений
├── toast.js                # Уведомления
├── .nojekyll               # Для GitHub Pages
├── .gitignore              # Игнорируемые файлы
├── README.md               # Документация
├── .github/workflows/      # GitHub Actions
│   └── deploy.yml
├── assets/                 # Изображения
│   ├── logo.png, SS.png, load.png, bg.png
│   ├── nominations/        # Изображения номинаций
│   └── candidates/         # Фотографии кандидатов
└── bot/                    # Telegram бот
    ├── worker.js           # Production код бота
    ├── wrangler.toml       # Конфигурация Cloudflare
    ├── package.json
    ├── deploy.bat          # Деплой бота
    └── README.md
```

### Development файлы (в папке `_dev/`, не на GitHub):
```
_dev/
├── README.md               # Описание dev файлов
├── deploy-site.bat         # Деплой сайта
├── deploy-all.bat          # Деплой всего
├── DEPLOY_GUIDE.md         # Инструкция по деплою
├── CHANGELOG.md            # История изменений
├── server.py               # Локальный сервер
└── bot/                    # Dev файлы бота
    ├── local-bot.js        # Локальный сервер бота
    ├── test-bot.bat        # Запуск локального бота
    ├── LOCAL_TESTING.md    # Инструкция по тестированию
    └── SETUP_GUIDE.md      # Инструкция по настройке
```

## 🎯 Использование

### Для пользователей:
1. Открой сайт
2. Выбери кандидата в каждой номинации
3. Нажми "Подтвердить выбор"
4. Повтори для всех номинаций

### Для администратора:
1. Открой `/admin.html`
2. Введи пароль
3. Смотри результаты в реальном времени
4. Используй "🗑️ Сбросить все голоса" для нового голосования

## 🔒 Защита от накрутки

Система использует двойную защиту:
- **localStorage** - блокирует повторное голосование в браузере
- **Firebase fingerprint** - отслеживает уникальные устройства
- **Firebase Rules** - запрещает повторную запись на уровне базы данных

Даже если очистить localStorage, Firebase помнит кто голосовал.

## 🛠️ Технологии

- HTML5, CSS3, JavaScript (ES6+ Modules)
- Firebase Realtime Database
- GitHub Pages + GitHub Actions
- Cormorant Garamond шрифт

## 📝 Номинации

- 🚗 Завоз года
- 🍺 Алкаш года
- 👑 Король класса
- 👸 Королева класса
- 📚 Любимец учителей
- 🎮 Лучший игрок Clash Royale
- 🙈 Мистер "Да это не я!"
- 😎 Нормис
- 💕 Мисис лучшая подружка

## 🐛 Решение проблем

**Ошибка загрузки результатов:**
- Проверь Firebase Rules (должен быть `.read: true` для votes)
- Проверь firebaseConfig в config.js

**Голоса не сохраняются:**
- Проверь Firebase Rules (должен быть `.write: true` или с датой)
- Открой консоль браузера (F12) для деталей

**Сбросить localStorage:**
```javascript
localStorage.clear()
```

## 📄 Лицензия

Создано для Slay School. Используй свободно!

---

Сделано с ❤️ для Slay School
